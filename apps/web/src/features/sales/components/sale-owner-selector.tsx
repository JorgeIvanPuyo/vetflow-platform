"use client";

import { OwnerSelector, type OwnerSelectorProps } from "@/components/owner-selector";

export function SaleOwnerSelector(props: OwnerSelectorProps) {
  return <OwnerSelector {...props} />;
}
